import {
  CalendarPlus,
  Camera,
  CreditCard,
  FileSpreadsheet,
  ListPlus,
  MessageCircle,
  ScanLine,
} from "lucide-react";

export const quickActionIcons = {
  scan: ScanLine,
  chat: MessageCircle,
  task: ListPlus,
  meeting: CalendarPlus,
  payments: CreditCard,
  import: FileSpreadsheet,
  camera: Camera,
};
