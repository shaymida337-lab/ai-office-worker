services:
  - type: web
    name: ai-office-worker-backend
    runtime: node
    plan: free
    region: oregon
    autoDeploy: true
    buildCommand: npm install --include=dev && npm run db:generate -w backend && npm run build -w backend
    startCommand: npm run db:migrate:deploy -w backend && npm run start -w backend
    healthCheckPath: /health
    envVars:
      - key: NODE_VERSION
        value: "22"
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        generateValue: true
      - key: FRONTEND_URL
        sync: false
      - key: GOOGLE_CLIENT_ID
        sync: false
      - key: GOOGLE_CLIENT_SECRET
        sync: false
      - key: GOOGLE_REDIRECT_URI
        sync: false
      - key: GOOGLE_INTEGRATION_REDIRECT_URI
        sync: false
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: ANTHROPIC_MODEL
        value: claude-haiku-4-5-20251001
      - key: CRON_SECRET
        generateValue: true
      - key: TWILIO_ACCOUNT_SID
        sync: false
      - key: TWILIO_AUTH_TOKEN
        sync: false
      - key: TWILIO_WHATSAPP_FROM
        sync: false
      - key: OWNER_WHATSAPP_NUMBER
        sync: false
      - key: GOOGLE_DRIVE_ROOT
        value: AI Office Worker

  - type: web
    name: ai-office-worker-frontend
    runtime: node
    plan: free
    region: oregon
    autoDeploy: true
    buildCommand: npm install --include=dev && npm run build -w frontend
    startCommand: cd frontend && npx next start -p $PORT
    healthCheckPath: /
    envVars:
      - key: NODE_VERSION
        value: "22"
      - key: NODE_ENV
        value: production
      - key: NEXT_PUBLIC_API_URL
        value: https://ai-office-worker-backend.onrender.com
