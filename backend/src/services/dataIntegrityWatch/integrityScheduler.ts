/**
 * Phase 2.3B — periodic scheduling deferred.
 * Manual `GET /api/integrity/watch` is the only production entry point in 2.3A.
 */
export const INTEGRITY_SCHEDULER_PLACEHOLDER = "phase-2.3b-scheduler" as const;
